var React    = require('react');
var ReactDOM = require('react-dom');
// var Button = ReactBootstrap.Button;

var RouterFront = require('./routerFront');

ReactDOM.render(
  <RouterFront/>
  ,
  document.getElementById('content')
);
